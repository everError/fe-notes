import type { ComponentMeta } from '@ide-demo/editor';

export const AInputNumberMeta: ComponentMeta = {
  name: 'AInputNumber',
  category: '입력',
  icon: '#',
  description: '숫자 입력 — AFormField 내장',
  tagName: 'AInputNumber',
  canHaveChildren: false,
  defaultProps: {
    label: '숫자',
    labelPosition: 'float',
    labelWidth: '120px',
    disabled: false,
    readonly: false,
    invalid: false,
    required: false,
    fluid: false,
    mode: 'decimal',
    useGrouping: true,
    showButtons: false,
    buttonLayout: 'stacked',
    variant: '',
    helpText: '',
    errorMessage: '',
    customClass: '',
  },
  props: {
    label: { type: 'text', label: '라벨' },
    labelPosition: {
      type: 'select',
      label: '라벨 위치',
      options: ['float', 'vertical', 'horizontal'],
      defaultValue: 'float',
    },
    labelWidth: { type: 'text', label: '라벨 너비', defaultValue: '120px' },
    variant: {
      type: 'select',
      label: '스타일 변형',
      options: ['', 'outlined', 'filled'],
    },
    mode: {
      type: 'select',
      label: '포맷 모드',
      options: ['decimal', 'currency'],
      defaultValue: 'decimal',
    },
    min: { type: 'number', label: '최솟값' },
    max: { type: 'number', label: '최댓값' },
    step: { type: 'number', label: '증감 단위' },
    minFractionDigits: { type: 'number', label: '최소 소수점' },
    maxFractionDigits: { type: 'number', label: '최대 소수점' },
    prefix: { type: 'text', label: '접두사', description: "예: '$'" },
    suffix: { type: 'text', label: '접미사', description: "예: 'kg'" },
    currency: {
      type: 'text',
      label: '통화 코드',
      description: "mode='currency' 시 ISO 4217 (예: KRW, USD)",
    },
    locale: { type: 'text', label: '로케일', description: "예: 'ko-KR'" },
    useGrouping: {
      type: 'boolean',
      label: '천 단위 구분자',
      defaultValue: true,
    },
    showButtons: { type: 'boolean', label: '증감 버튼', defaultValue: false },
    buttonLayout: {
      type: 'select',
      label: '버튼 배치',
      options: ['stacked', 'horizontal', 'vertical'],
      defaultValue: 'stacked',
    },
    placeholder: { type: 'text', label: '플레이스홀더' },
    disabled: { type: 'boolean', label: '비활성화', defaultValue: false },
    readonly: { type: 'boolean', label: '읽기 전용', defaultValue: false },
    invalid: { type: 'boolean', label: '유효성 실패', defaultValue: false },
    required: { type: 'boolean', label: '필수', defaultValue: false },
    fluid: { type: 'boolean', label: '부모 너비 맞춤', defaultValue: false },
    helpText: { type: 'text', label: '도움말' },
    errorMessage: { type: 'text', label: '에러 메시지' },
    customClass: { type: 'text', label: 'CSS 클래스' },
    modelValue: {
      type: 'binding',
      label: '값 (v-model)',
      bindingFilter: ['ref'],
    },
  },
  events: [
    { name: 'focus', label: '포커스', payload: 'Event' },
    { name: 'blur', label: '포커스 해제', payload: 'Event' },
  ],
  slots: [],
};
